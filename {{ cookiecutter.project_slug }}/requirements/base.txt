# Base requirements for {{ cookiecutter.project_name }}

# Django framework
django>=5.1,<5.2 # Use Django 5.1.x (latest stable)

# API Framework
django-ninja>=1.3,<2.0

# Celery and related packages
celery>=5.4,<6.0
redis>=5.1,<6.0 # Python client for Redis (used by Celery)
django-celery-beat>=2.7,<3.0 # Database-backed periodic task scheduler
django-celery-results>=2.5,<3.0 # Celery result backend using Django ORM

# Database driver (PostgreSQL)
psycopg2-binary>=2.9,<3.0 # Use binary for easier installation

# Cache backend (Redis)
django-redis>=5.4,<6.0

# JWT Authentication
djangorestframework-simplejwt>=5.3,<6.0

# OAuth2 Authentication
django-oauth-toolkit>=2.3,<3.0 # OAuth2 provider and consumer
requests-oauthlib>=2.0,<3.0 # OAuth2 client library
social-auth-app-django>=5.4,<6.0 # Social authentication (Google, GitHub, etc.)

# Object-level Permissions
django-guardian>=2.4,<3.0

# Environment variable management
python-decouple>=3.8,<4.0
# Optional: For parsing DATABASE_URL format easily
dj-database-url>=2.2,<3.0

# WSGI server for production
gunicorn>=22.0,<23.0