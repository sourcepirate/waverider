# Base requirements for {{ cookiecutter.project_name }}
#
# DEPRECATED: Use `uv sync` with pyproject.toml instead.
# These files are kept for pip compatibility only.
#
# See pyproject.toml for the authoritative dependency list.
